<form id="{formElements}" class="{table}" enctype="multipart/form-data" {method} {action}>
    <div>
        <fieldset>
            <legend>{legend}</legend>
            {submenu_items}
        </fieldset>
    </div>
    {hidden}
</form>
{add_elements}